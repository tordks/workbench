# CONTEXT.md

The ubiquitous language of this repo. Each entry points at the one file that *defines*
the term — this is an index, not a second home for the definition (that would widen
blast radius, `wiki/concepts/blast-radius.md`). Use these terms as named; don't drift
to synonyms.

| term | gloss | defined in |
|---|---|---|
| blast radius | state each fact once, refer to it by stable name | `wiki/concepts/blast-radius.md` |
| map / MOC | curated entry point for a theme; minted bottom-up on crowding | `wiki/_schema.md` (Links) |
| cascade | `wiki-query`'s bounded single-pass retrieval | `skills/wiki/wiki-query/SKILL.md` |
| capture / query / ingest / lint | the four wiki operations, one skill each | `wiki/_schema.md` (Operations) |
| source (immutable) | captured raw external material, read but never rewritten | `wiki/_schema.md` (three layers) |
| inbox | staging lane for raw captures awaiting ingest | `wiki/_schema.md` (three layers) |
| the dividing line | knowledge lives in `wiki/`, skills are tooling | `CLAUDE.md` |
